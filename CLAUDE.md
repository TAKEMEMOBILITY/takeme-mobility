# MANDATORY RULES — READ BEFORE EVERY TASK

## After EVERY change, no exceptions:
git add -A && git commit -m "fix/feat: [description]" && git push origin main

## Never say "done" without showing git push success output.
## Vercel auto-deploys on every push to main branch.
